name: CI/CD — Build · Trivy · Push · Deploy

on:
  push:
    branches: [master]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Kodu çek
        run: |
          git clone http://admin:Admin1234!@host.docker.internal:30880/admin/gaussian-plume.git .
          git checkout ${{ gitea.sha }}

      - name: Docker image build
        run: |
          docker build -t host.docker.internal:5000/gaussian-plume:${{ gitea.sha }} .
          docker tag host.docker.internal:5000/gaussian-plume:${{ gitea.sha }} host.docker.internal:5000/gaussian-plume:latest

      - name: Trivy — Zafiyet & Secret & Konfigürasyon Taraması
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v /tmp/trivy-cache:/root/.cache/trivy \
            aquasec/trivy:latest image \
            --exit-code 0 \
            --scanners vuln,secret,misconfig \
            --severity MEDIUM,HIGH,CRITICAL \
            --format table \
            host.docker.internal:5000/gaussian-plume:${{ gitea.sha }}

      - name: Registry push
        run: |
          docker push host.docker.internal:5000/gaussian-plume:${{ gitea.sha }}
          docker push host.docker.internal:5000/gaussian-plume:latest

      - name: Kubernetes deploy
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          curl -sLO "https://dl.k8s.io/release/v1.28.0/bin/linux/amd64/kubectl"
          chmod +x kubectl
          mkdir -p ~/.kube
          echo "$KUBE_CONFIG" | base64 -d > ~/.kube/config
          sed -i 's/kubernetes.docker.internal/host.docker.internal/g' ~/.kube/config
          sed -i 's/127.0.0.1/host.docker.internal/g' ~/.kube/config
          ./kubectl apply -f k8s/deployment.yaml --namespace=gaussian-plume --insecure-skip-tls-verify
          ./kubectl apply -f k8s/service.yaml --namespace=gaussian-plume --insecure-skip-tls-verify
          ./kubectl rollout restart deployment/gaussian-plume --namespace=gaussian-plume --insecure-skip-tls-verify
