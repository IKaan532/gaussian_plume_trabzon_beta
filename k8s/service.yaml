apiVersion: v1
kind: Service
metadata:
  name: gaussian-plume
  namespace: gaussian-plume
spec:
  type: NodePort
  selector:
    app: gaussian-plume
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30501
